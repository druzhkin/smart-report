# Production deps for Railway deploy of smart_report.api.main:app.
# Pinned floors mirror pyproject.toml [project.dependencies] + the
# additional runtime deps the FastAPI surface uses but pyproject hasn't
# yet declared (the project was previously a CLI-first package).
#
# Dev/test deps (pytest, ruff) live in pyproject.toml [project.optional-dependencies.dev].

fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.6
httpx>=0.27
python-dotenv>=1.0
click>=8.1
json-repair>=0.30
python-docx>=1.1
valyu>=2.9
pyyaml>=6.0
requests>=2.31
itsdangerous>=2.1
bcrypt>=4.0
psycopg[binary,pool]>=3.1
