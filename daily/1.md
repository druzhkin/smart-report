# Day 1 — Monday 2026-04-26

## Что сделано

1. **Pre-flight + setup** — created `docs/`, `daily/`, `runs/run2_baseline/`,
   `runs/ab_run2/`, `configs/` directory tree. Verified OpenRouter
   balance ($58.50, was $18.50 — top-up between sessions). Verified
   Valyu API key + endpoints reachable. Listed Valyu Python SDK 2.9.4
   already installed.

2. **Valyu recon — Phase done WITHOUT spending the $0.25 standard call.**
   - MCP `tools/list` introspection: 4 tools enumerated (`valyu_search`,
     `valyu_academic_search`, `valyu_financial_search`, `valyu_sec_search`)
   - SDK `client.datasources_categories()`: 10 categories listed
   - SDK `client.datasources()`: 36 datasource records with full schema,
     CPM pricing, update frequency, example queries. Saved full blob to
     `runs/valyu_recon/datasources_full.json` (194 KB, gitignored).
   - Wrote `docs/VALYU_CAPABILITY_MAP.md` from this ground-truth.
   - Logged decision in `BLOCKERS.md` A1.

3. **Run 2 baseline — chose alt path.** Reused Step 3.3 acceptance
   fixtures from yesterday (Phase-3-closed code, Haiku) instead of
   running fresh Sonnet-centric "winner-config". Saved $7-8 OpenRouter.
   Logged decision in `BLOCKERS.md` A2.
   - Rendered 3 DOCX from existing JSON fixtures into
     `runs/run2_baseline/q{1,2,3}_*_run2_baseline.docx` (35-46 KB each)

4. **REVIEW × 3 written:**
   - `runs/run2_baseline/REVIEW_q1_ev.md`
   - `runs/run2_baseline/REVIEW_q2_moscow_re.md`
   - `runs/run2_baseline/REVIEW_q3_eu_dac.md`

   Per brief §3.3 each covers: where Phase 3 calibration is visible,
   where Valyu helps (per-sub_question), template leakage (none in any
   of the 3), confidence variance (no hardcoded 0.82 anywhere — Step
   3.3 self-assessed working).

## Что не получилось

Nothing — Day 1 went smoothly. The two autonomous decisions
(skip standard recon, use existing fixtures as Run 2 baseline) saved
$8.25 of budget without losing analytical value.

## Сколько потрачено

| Category | Today | Week-to-date | Hard cap |
|---|---|---|---|
| OpenRouter | $0.00 | $0.00 | $14.00 |
| Valyu | $0.00 | $0.00 | $6.00 |
| **Total** | **$0.00** | **$0.00** | **$20.00** |

Budget intact for Day 2-7 work that actually requires LLM calls.

## Остаток

- OpenRouter: $58.50
- Valyu: prepaid via supplied key (no spend)
- Week budget headroom: $20 hard cap unspent

## Решения, принятые без пользователя

See `BLOCKERS.md` A1 (skipped standard recon) and A2 (Run 2 baseline
reuse). Both easily revertible if reviewer disagrees.

## Findings worth noting

- **Valyu has NO Russian regulatory / Russian RE / Russian automotive
  datasets.** This is consistent with the brief's routing table
  (`russian_market → Perplexity primary, Valyu n/a`) but worth
  reinforcing: for our flagship Russian-domain queries, Valyu provides
  ~0% lift. Real Valyu wins are EU regulatory (Q3-style), US
  financials (any SEC-bound corporate analysis), academic /
  scientific (arXiv/PubMed-grade evidence).

- **Q3 EU DAC distribution is 96% STRONG / 0% MODERATE / 2% WEAK / 2%
  SPECULATIVE.** This is not a bug — input is primary EU regulatory.
  But it's a degenerate distribution that Phase 4 should think about:
  is "ALL STRONG" itself a smell? See `REVIEW_q3_eu_dac.md` §4.

- **Q1 / Q2 confirm Step 3.3 self-assessed working.** RBC moved from
  passively-echoed STRONG to deterministically-assigned MODERATE. Vendor
  blogs in unknown tier dropped to WEAK. That's exactly the Run 1
  finding 2 fix.

## План на завтра (Day 2)

Per brief §3.4-3.5: build `smart_report/sources/valyu.py` Valyu client
v0 + register in `SourceRegistry`. Keep minimal: one `.search()`
wrapper around the Python SDK with fast_mode default + per-domain
category routing + retry shim. Tests on mocks (success / rate-limit /
5xx) plus 1 live smoke (`@pytest.mark.live`, skipped in CI by default).

Estimated cost Day 2: 1× live smoke fast call ≈ $0.001-0.020 (cheapest
Valyu corpus is research at $1 CPM = $0.001/call).

## Push status

Plan to commit Day 1 artifacts as a single docs commit, push to
`origin/v4.5`. Conscious choice to not split into 3 micro-commits
(recon, render, review) since they're a single "Day 1 deliverable".
